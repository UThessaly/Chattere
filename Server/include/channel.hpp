#pragma once

#include "packet.hpp"

namespace chattere
{
    // class Channel
    // {
    // public:
    //     Channel(Server *server, std::shared_ptr<protocol::BasicChannel> channel);

    //     std::int64_t GetId();

    // private:
    //     Server *server;
    //     std::shared_ptr<protocol::BasicChannel> channel;
    // };
} // namespace chattere